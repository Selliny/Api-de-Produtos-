spring:
  application:
    name: test
  datasource:
    url: jdbc:postgresql://localhost:5432/Test
    username: postgres
    password: mangodb
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
